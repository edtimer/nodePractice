const sayHi = (name) => {
  console.log(`hello there ${name}`);
};
const adder = (num1, num2) => {
  console.log(`Sum: ${num1 + num2}`);
};
const adde = (n) => {
  console.log(`Hello ${n}`);
};
module.exports = sayHi;
